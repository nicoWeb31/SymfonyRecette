{% extends 'base.html.twig' %}

{% block title %}Modifications creation des Types{% endblock %}
{% block monTitle %} Type{{type.libelle}} {% endblock %}

{% block body %}
<div class="container">
{{form_start(form)}}
<div class="row">
    <div class="col-6">

        <img src="{{asset('img/types/' ~ type.imgage)}}" alt="" style="width: 90px">

    </div>
    <div class="col-6">
        {{form_widget(form)}}
    </div>
    <button type="submit" class="btn btn-primary"> enregister</button>
    {{form_end(form)}}
</div>



</div>

{% endblock %}

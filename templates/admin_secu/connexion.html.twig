{% extends 'base.html.twig' %}

{% block title %}Hello AlimentController!{% endblock %}
{% block monTitle %}LOGIN{% endblock %}

{% block body %}
{% if error  %}
    <div class="alert alert-danger">
        {{ error.messageKey | trans(error.messagedata,'security')}}
    </div>
{% endif %}

<form action="" method="POST">
    <div class="form-group">
        <label for="{{path('login')}}">login</label>
        <input type="text" required name="_username" class="for-control">
    </div>

    <div class="form-group">
        <label for="">password</label>
        <input type="password" required name="_password" class="for-control">
    </div>
    <button type="submit" class="btn btn-primary"> login </button>

</form>


{% endblock %}